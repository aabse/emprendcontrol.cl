---
import AddressIcon from "./icons/AddressIcon.astro"
import FacebookIcon from "./icons/FacebookIcon.astro"
import InstagramIcon from "./icons/InstagramIcon.astro"
import MailIcon from "./icons/MailIcon.astro"
import PhoneIcon from "./icons/PhoneIcon.astro"
import ScheduleIcon from "./icons/ScheduleIcon.astro"

const contact = {
  phone: '+57 310 123 4567',
  email: 'contacto@plagas.com.co',
  address: 'Salamanca, Chile',
  businessHours: [
    'Lunes a Viernes: 8:00 AM - 6:00 PM',
    'Sábado: 9:00 AM - 2:00 PM',
    'Domingos y festivos: Cerrado'
  ],
  socialMedia: {
    facebook: 'https://www.facebook.com/plagas.com.co',
    twitter: 'https://twitter.com/plagascomco',
    instagram: 'https://www.instagram.com/plagascomco/'
  }
}
---
<section id="contacto" class="section bg-white relative overflow-hidden">
  <!-- Formas decorativas -->
  <div class="absolute top-40 right-0 w-96 h-96 bg-green-50 rounded-full opacity-70 blur-3xl -z-10"></div>
  <div class="absolute bottom-40 left-0 w-64 h-64 bg-yellow-50 rounded-full opacity-70 blur-3xl -z-10"></div>
  
  <div class="container-custom relative">
    <div class="text-center mb-16" data-aos="fade-up">
      <h2 class="section-title mx-auto">Contáctenos</h2>
      <p class="text-gray-600 mt-6 max-w-3xl mx-auto">
        Estamos listos para ayudarle con cualquier problema de plagas. Contáctenos hoy para una consulta gratuita.
      </p>
    </div>
    
    <div class="grid lg:grid-cols-5 gap-8">
      <!-- Contact Form -->
      <div class="lg:col-span-3" data-aos="fade-right">
        <div class="card border-none shadow-xl">
          <h3 class="text-2xl font-bold text-gray-800 mb-6">Envíenos un mensaje</h3>
          <form class="space-y-5">
            <div class="grid sm:grid-cols-2 gap-5">
              <div>
                <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Nombre</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors"
                  placeholder="Su nombre"
                  required
                />
              </div>
              <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors"
                  placeholder="Su email"
                  required
                />
              </div>
            </div>
            
            <div>
              <label for="phone" class="block text-sm font-medium text-gray-700 mb-1">Teléfono</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors"
                placeholder="Su teléfono"
              />
            </div>
            
            <div>
              <label for="service" class="block text-sm font-medium text-gray-700 mb-1">Servicio de interés</label>
              <select
                id="service"
                name="service"
                class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors"
              >
                <option value="">Seleccione un servicio</option>
                <option value="residential">Fumigación Residencial</option>
                <option value="commercial">Fumigación Comercial</option>
                <option value="preventive">Control Preventivo</option>
                <option value="other">Otro</option>
              </select>
            </div>
            
            <div>
              <label for="message" class="block text-sm font-medium text-gray-700 mb-1">Mensaje</label>
              <textarea
                id="message"
                name="message"
                rows="4"
                class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors"
                placeholder="Describa su problema de plagas"
                required
              ></textarea>
            </div>
            
            <button
              type="submit"
              class="w-full btn btn-primary"
            >
              Enviar Mensaje
            </button>
          </form>
        </div>
      </div>
      
      <!-- Contact Info -->
      <div class="lg:col-span-2" data-aos="fade-left">
        <div class="card h-full border-none shadow-xl">
          <h3 class="text-2xl font-bold text-gray-800 mb-6">Información de Contacto</h3>
          
          <div class="space-y-6">
            <div class="flex items-start">
              <div class="w-14 h-14 bg-gradient-to-br from-green-50 to-green-100 rounded-xl flex items-center justify-center mr-4 text-green-500 flex-shrink-0 shadow-md">
                <PhoneIcon />
              </div>
              <div>
                <h4 class="font-bold text-gray-800 text-lg">Teléfono</h4>
                <p class="text-gray-600 mt-1">{contact.phone}</p>
              </div>
            </div>
            
            <div class="flex items-start">
              <div class="w-14 h-14 bg-gradient-to-br from-green-700/5 to-green-700/10 rounded-xl flex items-center justify-center mr-4 text-green-700 flex-shrink-0 shadow-md">
                <MailIcon />
              </div>
              <div>
                <h4 class="font-bold text-gray-800 text-lg">Email</h4>
                <p class="text-gray-600 mt-1">{contact.email}</p>
              </div>
            </div>
            
            <div class="flex items-start">
              <div class="w-14 h-14 bg-gradient-to-br from-yellow-50 to-yellow-100 rounded-xl flex items-center justify-center mr-4 text-yellow-500 flex-shrink-0 shadow-md">
                <AddressIcon />
              </div>
              <div>
                <h4 class="font-bold text-gray-800 text-lg">Dirección</h4>
                <p class="text-gray-600 mt-1">{contact.address}</p>
              </div>
            </div>
            
            <div class="flex items-start">
              <div class="w-14 h-14 bg-gradient-to-br from-gray-100 to-gray-200 rounded-xl flex items-center justify-center mr-4 text-gray-700 flex-shrink-0 shadow-md">
                <ScheduleIcon />
              </div>
              <div>
                <h4 class="font-bold text-gray-800 text-lg">Horario de Atención</h4>
                {contact.businessHours.map((businessHour) => (
                  <p class="text-gray-600">{businessHour}</p>
                ))}
              </div>
            </div>
          </div>
          
          <div class="mt-8">
            <h4 class="font-bold text-gray-800 mb-4">Síguenos en redes sociales</h4>
            <div class="flex space-x-4">
              <a href={contact.socialMedia.facebook} class="w-12 h-12 rounded-full bg-gray-50 flex items-center justify-center text-gray-600 hover:bg-green-500 hover:text-white transition-colors shadow-md">
                <FacebookIcon />
              </a>
              <a href={contact.socialMedia.instagram} class="w-12 h-12 rounded-full bg-gray-50 flex items-center justify-center text-gray-600 hover:bg-green-500 hover:text-white transition-colors shadow-md">
                <InstagramIcon />
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
